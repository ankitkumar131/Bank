import './globals.css';
import { Inter } from 'next/font/google';
import Header from '@/components/Header';
import Footer from '@/components/Footer';

const inter = Inter({ subsets: ['latin'] });

export const metadata = {
  title: 'Parra Verla Canca - Multipurpose Primary Agriculture Co-operative Society Ltd.',
  description: 'Parra Verla Canca - Multipurpose Primary Agriculture Co-operative Society Ltd. Provides comprehensive banking and agricultural services.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );
}
